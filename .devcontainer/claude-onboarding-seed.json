{
  "//": "Minimal onboarding seed for headless claude-code in-container (#6273/#6343). Contains NO real identity — claude-code populates userID/auth state at first run. Do NOT paste a real ~/.claude.json userID/anonymousId here (identity fingerprint leak). Gitignored + mounted READ-ONLY at .claude-seed then copied to a writable /home/dev/.claude.json so container runs never pollute the source (#6140). Auto-generated by generate-repo-configs.sh.",
  "hasCompletedOnboarding": true,
  "bypassPermissionsModeAccepted": true,
  "projects": {
    "/workspace": {
      "hasTrustDialogAccepted": true
    }
  }
}